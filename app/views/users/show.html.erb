<%= render 'layouts/aside' %>
<div class="main">
  <h1>
      <%= gravatar_for @user %>
      <%# <%= @user.posts.title %> 
      <%= @user.name %>
      <%  @user.email %>
  </h1>
  <h1><%= @user.posts.content %></h1>
  <div class="main-content" >
    <% if @user.posts.any? %>
      <h3>投稿数(<%= @user.posts.count %>)</h3>
      <ol class="#">
        <%= render @posts %>
      <ol>
    <% end %>
  </div>
</div>
